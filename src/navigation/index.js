import React, { Fragment } from "react";
import DefaultRoutes from "./default";

const Navigation = () => {
  return (
    <Fragment>
      <DefaultRoutes />
    </Fragment>
  );
};

export default Navigation;
